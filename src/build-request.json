{
  "kind": "build_request",
  "title": "Replace Product Banner Sample 1–4 images in Explore Our Work and rebuild/deploy",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Replace the existing four static fallback Product Banner sample images used by the sample selector (paths referenced by `useSampleSlots()` in `frontend/src/hooks/useProductBannerSamples.ts`) with the user-uploaded images, in sequence, so that Explore Our Work → Product Banner → Sample 1–4 shows the new images even when the backend has no stored samples.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-12"
        ],
        "quotes": [
          "existing sample photos  ko explore our work → product banner → sample 1 sample 2 sample 3 sample 4 hata kar  replace karna hai",
          "Aap sequence se laga do poori tarah se delete karna",
          "Build shuru karo"
        ]
      },
      "acceptanceCriteria": [
        "The following public URLs still exist and return images, but now display the newly provided designs: `/assets/generated/sample1.dim_1200x800.jpg`, `/assets/generated/sample2.dim_1200x800.jpg`, `/assets/generated/sample3.dim_1200x800.jpg`, `/assets/generated/sample4.dim_1200x800.jpg`.",
        "The four new images are applied in sequence using the first four uploaded files in the upload order: (1) `Screenshot_20260213-192911 (1).png` → Sample 1, (2) `Screenshot_20260213-192903 (1).png` → Sample 2, (3) `Screenshot_20260213-192858 (1).png` → Sample 3, (4) `Screenshot_20260213-192911 (1)-1.png` → Sample 4.",
        "Old sample images are no longer present in the repository under those sample1–sample4 filenames (they are overwritten/replaced).",
        "On the Dashboard ordering flow sample selector (`frontend/src/components/ProductSampleSelector.tsx`), selecting Sample 1–4 shows the replaced images (via fallbackUrl when backend samples are null)."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Rebuild and redeploy the app after the image replacement, and fix any build/deploy errors that prevent the draft app from updating.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-1",
          "msg-12"
        ],
        "quotes": [
          "🙏 We hit a snag during deployment",
          "Build shuru karo"
        ]
      },
      "acceptanceCriteria": [
        "Build completes successfully without errors.",
        "Deployment completes successfully and the updated draft/preview app reflects the new Sample 1–4 images in the Product Banner sample selector."
      ]
    }
  ],
  "constraints": [
    "Use English for any user-facing text.",
    "Do not modify any files under `frontend/src/components/ui` (read-only Shadcn components).",
    "Do not modify any files listed in `frontend.immutablePaths` in SYSTEM_CONTEXT.",
    "Backend must remain single-actor (all Motoko logic in `backend/main.mo`); add/adjust `backend/migration.mo` only if required for state compatibility."
  ],
  "nonGoals": [
    "Do not change the Product Banner design preview images (`/assets/generated/product-banner-original...` and `/assets/generated/product-banner-duplicate-with-photo...`).",
    "Do not redesign the layout or add new product sections.",
    "Do not add new authentication methods beyond Internet Identity.",
    "Do not implement new admin features (admin upload UI and backend APIs already exist)."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}